# @kleptowriter/adapter-opencode

OpenCode harness adapter — registers Kleptowriter agents as OpenCode sub-agents.

## Exports

- `OpenCodeAdapter` (implements `HarnessAdapter`)

Status: Stub — harness integration pending.
